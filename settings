-- Viewer settings file
-- Do not alter these settings manually, they should be edited only in the application


main = {
    version = 0.01
    safeShutdown = false
    }
gui = {
    version = 0.01,
    menuBarColor = colors.red,
    menuTextColor = colors.blue,
    menuLocation = "top"
    backgroundColor = colors.black,
    textColor = colors.white
    }
update = {
    version = 0.01
    }
